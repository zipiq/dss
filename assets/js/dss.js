$( function() {
        
        

        



//        $("#content").html('<a id="riverroad" href="#" title="" >image of 1 Maple St.</a>');
//        $("#content #riverroad").tooltip({ content: '<img src="../assets/img/dss/projects/3.jpg" />' }); 

        
        //$( '#photoCredit' ).html( 'Architect of Record: <a href="https://d2groups.com/" target="_blank">D2 Groups</a>' );

        /*$( '.fancynavbar-toggler' ).click(function() {
          $( '#photoCredit' ).toggle();
        });*/



/*
        var owl = $('.owl-carousel');
        owl.owlCarousel();
        
        owl.on('changed.owl.carousel', function(event) {

            var element   = event.target;         // DOM element, in this example .owl-carousel
            var name      = event.type;           // Name of the event, in this example dragged
            var namespace = event.namespace;      // Namespace of the event, in this example owl.carousel
            var items     = event.item.count;     // Number of items
            var item      = event.item.index;     // Position of the current item
            
            var credit = "";

            switch(item) {
                case 2:
                    var title = $( "#headerImage_1" ).attr( "data-original-title" );
                    console.log(title);
                    $( "#photoCredit" ).html( title );
                  break;
                case 3:
                    var title = $( "#headerImage_2" ).attr( "data-original-title" );
                    console.log(title);
                    $( "#photoCredit" ).html( title );
                  break;
                case 4:
                    var title = $( "#headerImage_3" ).attr( "data-original-title" );
                    console.log(title);
                    $( "#photoCredit" ).html( title );
                  break;
                default:
                  // code block
              }

            console.log(item);
        })
    */
} );
